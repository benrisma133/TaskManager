<UserControl x:Class="TaskManagerUI.Controls.Components.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TaskManagerUI.Helpers"
             x:Name="Root">

    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <StackPanel>

        <!-- Label -->
        <TextBlock Text="Color"
                   FontSize="13"
                   FontWeight="Medium"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Margin="0,0,0,8"/>

        <!-- Color Grid -->
        <ItemsControl x:Name="ColorGrid">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="36"
                            Height="36"
                            Margin="4"
                            Tag="{Binding Hex}"
                            Click="ColorSwatch_Click"
                            Cursor="Hand"
                            BorderThickness="0"
                            Background="Transparent">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <!-- Swatch -->
                                    <Border x:Name="Swatch"
                                            Background="{Binding Hex}"
                                            CornerRadius="6"
                                            BorderThickness="2.5"
                                            BorderBrush="Transparent"/>

                                    <!-- Checkmark -->
                                    <Viewbox Width="14" Height="14"
                                             Visibility="{Binding IsSelected,
                                                Converter={StaticResource BoolToVisibilityConverter}}">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="M4 12L9 17L20 6"
                                                  Stroke="White"
                                                  StrokeThickness="2.5"
                                                  StrokeLineJoin="Round"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  Fill="Transparent"/>
                                        </Canvas>
                                    </Viewbox>
                                </Grid>

                                <ControlTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter TargetName="Swatch" Property="BorderBrush"
                                                Value="White"/>
                                        <Setter TargetName="Swatch" Property="BorderThickness"
                                                Value="2.5"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Swatch" Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Selected Color Preview -->
        <StackPanel Orientation="Horizontal"
                    Margin="0,10,0,0"
                    VerticalAlignment="Center">
            <Border x:Name="SelectedPreview"
                    Width="28"
                    Height="28"
                    CornerRadius="6"
                    Margin="0,0,10,0"/>
            <TextBlock x:Name="SelectedHexText"
                       FontSize="13"
                       FontFamily="Consolas"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>
        </StackPanel>

    </StackPanel>
</UserControl>